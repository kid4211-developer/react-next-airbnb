export interface IProps {
  closeModal: () => void;
}

export interface infoType {
  email: string;
  lastName: string;
  firstName: string;
  password: string;
}

export interface birthdayType {
  day: string;
  month: string;
  year: string;
}
